Integrate Supabase into the ميدان app.

Use these credentials (add to Replit Secrets):
SUPABASE_URL = https://hnoqkcrzualzxkzmuwvp.supabase.co
SUPABASE_ANON_KEY = sb_publishable_eZVVmvhqAYdiyPfBWqddTw_yk_1JxJy

Steps:

1. Install @supabase/supabase-js package

2. Create supabase.ts client file using the env variables above

3. Create these tables in Supabase by running SQL:

CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  auth_id TEXT UNIQUE,
  username TEXT,
  avatar_url TEXT,
  total_wins INT DEFAULT 0,
  total_losses INT DEFAULT 0,
  streak_count INT DEFAULT 0,
  longest_streak INT DEFAULT 0,
  last_played DATE,
  is_premium BOOLEAN DEFAULT false,
  total_points INT DEFAULT 0,
  created_at TIMESTAMP DEFAULT now()
);

CREATE TABLE scores (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  username TEXT,
  category TEXT,
  score INT,
  game_mode TEXT,
  created_at TIMESTAMP DEFAULT now()
);

CREATE TABLE challenges (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  creator_id UUID REFERENCES users(id),
  creator_name TEXT,
  opponent_id UUID REFERENCES users(id),
  opponent_name TEXT,
  status TEXT DEFAULT 'pending',
  creator_score INT,
  opponent_score INT,
  category TEXT,
  created_at TIMESTAMP DEFAULT now()
);

4. Add Authentication:
   - Google OAuth login button "تسجيل الدخول بـ Google 🔵"
   - Guest mode button "العب كضيف 👤" (limited features)
   - Save user to users table on first login
   - Show user avatar from Google profile
   - Logout option in profile page

5. Replace ALL localStorage with Supabase:
   - streak_count → users table (update daily)
   - leaderboard → query scores table ORDER BY score DESC
   - challenge results → challenges table
   - premium status → users.is_premium field

6. Add username setup screen:
   - Shows after first Google login
   - "اختر اسم المستخدم" 
   - Validates: Arabic or English, 3-15 chars, unique
   - Shows availability check ✅ or ❌

7. Update profile page to show:
   - Real data from users table
   - Sync across all devices

Keep RTL Arabic UI. Dark gold/purple theme.
Show loading states in Arabic while fetching data.